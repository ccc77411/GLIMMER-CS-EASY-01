# part2_C语言基础  
## 1.变量类型  
### 什么是变量的类型？它为什么重要？  
- 变量的类型:规定了变量在内存中所占空间的大小以及存储数据的格式，限定了能存放什么样的数据。
- 重要性：决定了内存的分配，取值范围等防止数据溢出，类型错误。    
### 如果你想存放你的年龄（比如 18），应该选择哪种基本变量类型（如整数、浮点数）？  
- 选int（整型）。    
### 如果你想存放单词 “apple”，使用一个char类型的字符变量能实现吗？尝试编程验证你的想法，并思考正确的存储方式应该是什么？  
- 不能实现，因为char类型变量只有一个字节大小只能存放一个字符  
- 正确的存储方式：要用char数组`char str[] ="apple";`来存储末尾自动带`\0`字符串结束标记。  
## 2.数组的起始与边界  
### 数组的第一个元素下标通常是从 0 还是 1 开始？  
- 下标从0开始。 
### 访问数组元素时，下标超出其有效范围（例如，长度为 5 的数组访问 arr[5] 或 arr[-1]）会导致什么问题？  
-  可能会访问不属于这个数组的内存，使结果不可预测，可能出现乱码，程序奔溃等。   
### 为什么数组越界是一个常见的、危险的错误？  
- 因为C语言不会自动检查数组的边界，所以在编译的时候不会报错，但运行的时候会随机出错，很难找出bug。    
##  3.流程控制 - 循环结构  
### 循环结构（如 for, while）是如何控制代码块重复执行的？请分别简述它们的基本结构。    
- **for循环结构** ：`for(初始化，条件判断，迭代){执行语句}`
- **while循环结构**：`while（条件）{执行语句}`   
### for 循环的初始化、条件判断和迭代部分各自的作用是什么？  
- **初始化**:设置初始条件，在循环开始前执行一次。  
- **条件判断**：在每次循环前进行判断，判断是否执行循环。  
- **迭代**：在循环后执行，用来更新循环变量的值防止出现无限循环。    
### while 循环和 do…while 循环在首次执行条件判断上有何关键区别？  
- **while循环**是先进行条件判断，后执行。如果条件不成立则不会进入循环。  
- **do...while循环**是先进行一次循环，再来进行条件判断。  
####  while 循环和 do…while 循环结构异同演示  
- **while循环**  
    >\#include<stdio.h>  
    int main(){   
            int i=1;  
        int sum=0;  
        while(i<=10){  
            sum+=i;  
            i++;  
        }  
        printf("%d",sum);  
        return 0;  
    }    
- **do...while循环**  
    >\#include<stdio.h>  
    int main(){  
    int i=1;  
    int sum=0;  
    do{  
        sum+=i;  
        i++;  
    }while(i<=10);  
    printf("%d",sum);   
    return 0;  
}  
    
## 4.流程控制 - 逻辑表达式  
### 在条件判断（如 if, else if）和循环条件中使用的逻辑表达式（如`age > 18 && score >= 60`）与算术表达式（如`a + b * c`）有何本质区别(尝试从运算对象和运算结果分析)？  
- **算术表达式**:运算对象是数字，运算结果是数值。  
- **算术表达式**：运算对象是真假值，运算结果是真或假（0或1）。  
### 运算逻辑符&&,||,!的含义分别是什么？  
- **&&** 表示与，只有两个条件为真才为真  
- **||** 表示或，只要有一个条件为真即为真  
- **！** 表示非，表示取反，真变假，假变真。    


